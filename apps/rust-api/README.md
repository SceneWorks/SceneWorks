# SceneWorks Rust API

This binary is the initial Rust backend migration target. It is intentionally
not wired into Docker Compose or the default development runtime yet; the
current FastAPI service remains the backend until Rust endpoints reach parity.

Use the root Rust scripts to format, lint, test, and build this workspace.
