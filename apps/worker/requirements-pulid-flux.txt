# PuLID-FLUX face-identity adapter (sc-2012, epic 2003) — optional extras on top
# of requirements.txt. Install into the MAIN worker venv; coexists with the
# diffusers/torch stack (validated on Apple Silicon, sc-2012 spike).
#
#   pip install -r apps/worker/requirements.txt -r apps/worker/requirements-pulid-flux.txt
#
# timm + ftfy drive the vendored EVA02-CLIP-L-14-336 visual encoder; facexlib
# wraps face alignment + bisenet face parsing. insightface + onnxruntime overlap
# with InstantID (requirements-instantid.txt) — the shared sc-2009 antelopev2 pack
# is reused. accelerate + sentencepiece are pulled by the BFL flow loaders +
# T5-XXL tokenizer. einops is also imported by the vendored InstantID ip_adapter,
# but pinning it here keeps PuLID-FLUX installable on its own.
timm>=1.0
facexlib>=0.3
ftfy>=6.0
einops>=0.8
insightface>=1.0,<2
onnxruntime>=1.20
accelerate>=1.0
sentencepiece>=0.2
