{
  "identifier": "default",
  "description": "Local-only first-run capability for the bundled setup splash. Steady-state API-served UI permissions are not granted here because the sidecar port is discovered at runtime; setup.rs installs a dynamic capability for the exact health-checked http://127.0.0.1:<port> origin before navigating.",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "allow-start-setup",
    "allow-get-storage-setup",
    "allow-save-storage-setup",
    "allow-choose-folder"
  ]
}
