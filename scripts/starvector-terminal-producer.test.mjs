import assert from "node:assert/strict"; import { mkdtemp,mkdir,writeFile,readFile } from "node:fs/promises"; import { tmpdir } from "node:os"; import path from "node:path"; import test from "node:test"; import { acquireLease,inventory,preflight } from "./starvector-terminal-producer.mjs";
test("inventory seals bytes and lease is exclusive",async()=>{const root=await mkdtemp(path.join(tmpdir(),"svt-"));await writeFile(path.join(root,"a"),"x");assert.equal((await inventory(root)).entries.length,1);const release=await acquireLease(path.join(root,"lease"),"pin");await assert.rejects(()=>acquireLease(path.join(root,"lease"),"pin"),/already held/);await release();});
test("preflight refuses absent pinned contract or weights",async()=>{const root=await mkdtemp(path.join(tmpdir(),"svt-")),inf=path.join(root,"inference"),weights=path.join(root,"weights");await mkdir(path.join(inf,"release"),{recursive:true});await mkdir(weights);await writeFile(path.join(inf,"release/starvector-terminal-receipt-v1.schema.json"),"{}");await assert.rejects(()=>preflight({planPath:"release/starvector-terminal-campaign-v1.json",inferenceRoot:inf,weightsRoot:weights,command:["true"]}),/corpus/);});
