# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-31 00:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based API server, likely built with FastAPI, that serves as a backend orchestration layer for AI-driven image and scene generation workflows. It manages several interconnected domain entities: projects (organizational containers), assets (generated or uploaded media), characters with associated LoRA model bindings (fine-tuned generative model weights tied to specific characters), person tracking (associating detected individuals across images or scenes), image generation jobs (dispatched to background workers), and model manifests (configuration or registry data for generative models). The server coordinates between a persistent asset/project database and a worker job queue or execution system, acting as the control plane for a creative AI pipeline. There is no evidence of a frontend in this repository; it appears to expose an HTTP API consumed by external clients or tools. No cross-repository dependencies were identified, but the system implies downstream worker processes that consume job dispatches and upstream clients (UI, scripts, or other services) that submit generation requests and query results.

## Projects

### SceneWorks

SceneWorks is a Python-based API server (built with FastAPI, inferred from `Request` usage) that manages AI-driven scene/image generation workflows. It handles projects, assets, characters with LoRA model associations, person tracking, image generation jobs, and model manifests — acting as the backend orchestration layer for a creative AI pipeline that coordinates worker jobs, asset databases, and generative model configurations.

