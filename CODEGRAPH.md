# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-19 10:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python backend API server built with FastAPI that orchestrates AI-driven image and scene generation workflows. Its core responsibilities include: managing projects and their associated assets; tracking characters and LoRA fine-tuned diffusion model weights; queuing and dispatching generation jobs with worker lifecycle management; and streaming real-time status updates to clients via Server-Sent Events (SSE). The system appears to sit between a frontend creative tool and one or more diffusion model inference backends, coordinating the full pipeline from project setup through character-consistent image generation using LoRA adapters. There is only one project in the repository, so all functionality is self-contained within this single service. No cross-repository dependencies were identified, meaning external consumers (e.g., a frontend UI or a separate inference service) would interact with it purely through its HTTP/SSE API surface. The service likely depends on a diffusion model runtime (such as ComfyUI, Automatic1111, or a similar inference server) as an external backend, and on a database or file system for persisting project and asset state, though the specific integrations are not detailed in the provided evidence.

## Projects

### SceneWorks

SceneWorks is a Python-based API server (built with FastAPI) that manages AI-driven scene and image generation workflows. It handles project and asset management, character/LoRA model tracking, job queuing with worker lifecycle management, and Server-Sent Events for real-time updates. The project appears to be a backend service for a creative tool that uses diffusion models and LoRA fine-tuning for character-consistent image generation.

