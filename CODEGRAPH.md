# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-23 12:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a FastAPI-based web API service that orchestrates AI-assisted image and scene generation workflows. It manages structured projects containing characters, AI model manifests (including LoRA models), asset databases, person tracking data, and image generation jobs. The system coordinates background workers for processing generation requests, maintains persistent job state across requests, and delivers real-time status updates to clients via Server-Sent Events (SSE). The architecture centers on a single Python service that exposes REST endpoints for project and job management while delegating compute-intensive generation tasks to background processes. There are no identified cross-repository dependencies, suggesting this is a self-contained backend service. Likely consumers would be a frontend UI or CLI that submits generation jobs and streams progress, but no such consumers are directly evidenced in the repository.

## Projects

### SceneWorks

SceneWorks is a web API service (built with FastAPI, based on the Request parameter patterns) that manages AI-assisted scene/image generation workflows. It handles projects containing characters, AI model manifests (including LoRA models), asset databases, person tracking data, and image generation jobs. The system coordinates background workers, maintains persistent job state, and serves server-sent events (SSE) for real-time updates.

