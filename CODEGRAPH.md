# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-20 02:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based API backend, likely built with FastAPI, that serves as an orchestration layer for AI-assisted image and scene generation workflows. It manages several interconnected concerns: projects and assets (organizing generated content), character definitions linked to LoRA model weights (enabling character-consistent image synthesis), image generation jobs (queuing and tracking generation requests), person/subject tracking, and model manifests (cataloging available AI models). The system appears to expose REST and Server-Sent Events (SSE) endpoints to stream job progress or results to clients. There is only one project in the repository, so all functionality is contained within this single backend service. It depends on external AI inference infrastructure (likely diffusion model servers or APIs that consume the LoRA/model references it manages) and is itself likely consumed by a frontend creative tool or pipeline that drives scene composition with character-based image synthesis. No cross-repository dependencies were identified, so the full dependency surface — including the AI model serving layer and any client applications — is external and not directly observable from this codebase.

## Projects

### SceneWorks

SceneWorks is a Python-based API backend (built with FastAPI, inferred from Request objects and SSE patterns) that manages AI-assisted scene/image generation workflows. It handles projects, assets, character definitions with LoRA model links, image generation jobs, person tracking, and model manifests — suggesting it serves as an orchestration layer for a creative AI tool dealing with character-based image synthesis.

