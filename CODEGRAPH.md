# SceneWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-21 01:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based API service built on FastAPI that provides a backend for AI-driven image and scene generation workflows. It exposes REST endpoints for managing projects, assets (including characters and tracked person identities), and AI model manifests (with support for LoRA fine-tuning models). The service handles job queuing for image generation tasks and delivers real-time status updates to clients via Server-Sent Events (SSE). Architecturally, it functions as an orchestration layer: it organizes creative assets within named projects, references generative AI model configurations, dispatches generation jobs to a processing queue, and streams results or progress events back to consumers. There are no observed cross-repository dependencies, suggesting it operates as a self-contained service or is intended to be composed with external AI inference backends and storage systems. The primary technical responsibilities are project/asset lifecycle management, AI model manifest registration, asynchronous job dispatch, and event streaming.

## Projects

### SceneWorks

SceneWorks is a Python-based API service (built with FastAPI) that manages AI-driven scene and image generation workflows. It handles projects, assets (characters, person tracks), AI model manifests (including LoRA fine-tuning models), image generation job queuing, and real-time event streaming via Server-Sent Events (SSE). The system appears to support a creative/production pipeline where users work with characters, tracked persons, and generative AI models within named projects.

