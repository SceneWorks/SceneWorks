# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-27 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based backend service built with FastAPI that orchestrates AI-driven image and scene generation workflows. It provides a REST/SSE API layer managing several interconnected concerns: project and asset lifecycle management, character definitions linked to LoRA model associations, image generation job orchestration, person/entity tracking across scenes, and caching of model manifests. The architecture centers on coordinating AI image generation pipelines — likely interfacing with diffusion model inference backends — with persistent project storage. Core technical responsibilities include: (1) project and asset CRUD operations, (2) character/persona management with associated fine-tuned model (LoRA) bindings, (3) asynchronous job queuing and status tracking for image generation tasks, (4) Server-Sent Events (SSE) for real-time job progress streaming to clients, and (5) model manifest caching to avoid redundant lookups against an external model registry. The single-repository structure suggests all components are part of one deployable service rather than a microservices architecture. No cross-repository dependencies were identified, so external consumers (e.g., a frontend UI or CLI) and upstream inference backends (e.g., ComfyUI, A1111, or a custom diffusion server) are inferred but not directly observable in this codebase.

## Projects

### SceneWorks

SceneWorks is a Python-based API backend (built with FastAPI, inferred from Request/SSE patterns) that manages AI-driven scene/image generation workflows. It handles project and asset management, character definitions with LoRA model associations, image generation job orchestration, person tracking, and model manifest caching. The project appears to be a backend service coordinating AI image generation pipelines with persistent project storage.

