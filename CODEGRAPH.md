# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-21 15:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a FastAPI-based web API service that acts as a backend orchestration layer for AI-driven image and scene generation workflows. It manages several core domain entities: projects (likely grouping related generation work), assets (media files such as generated or uploaded images), character definitions (with associations to LoRA fine-tuned diffusion models), person tracking, image generation jobs, and model manifests (descriptors of available AI models). The service appears to coordinate diffusion model pipelines — accepting generation requests, dispatching jobs, and tracking results — rather than running inference directly. There is a single project in the repository with no detected cross-repository dependencies, suggesting it is a self-contained backend service. Likely consumers would be a front-end creative application or a pipeline runner that submits generation jobs and retrieves outputs. The architecture follows REST API conventions with FastAPI, and the domain model implies integration with external diffusion model infrastructure (e.g., Stable Diffusion with LoRA adapters).

## Projects

### SceneWorks

SceneWorks is a web API service (built with FastAPI based on the Request parameter patterns) that manages AI-driven scene/image generation workflows. It handles projects, assets (likely media files), character definitions with LoRA model associations, person tracking, image generation jobs, and model manifests — suggesting it is a backend for a creative AI application that orchestrates diffusion model pipelines.

