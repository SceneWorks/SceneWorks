# SceneWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-20 12:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based backend API service built with FastAPI that orchestrates a generative AI content production pipeline. It exposes REST endpoints for managing projects, characters, persons, AI image generation jobs, model/LoRA manifests, and asset indexes. The service coordinates between a file-system-backed project structure (storing scenes, characters, and generated assets), an in-process or persistent jobs store, and external worker processes that execute AI image generation tasks. Core responsibilities include: project lifecycle management (create, read, update, delete project workspaces), character and person tracking (associating identities with generated or imported assets), job queuing and status tracking for AI inference tasks (likely diffusion-model-based image generation), and manifest management for ML models and LoRA adapter weights. The architecture suggests a hub-and-spoke design where this API acts as the central coordinator — the file system holds project state, worker processes consume job entries to run generation, and clients (likely a UI or CLI) drive the pipeline through the REST API. There are no identified cross-repository dependencies, meaning this service is self-contained or relies on conventions (file paths, job schemas) rather than shared libraries to integrate with workers.

## Projects

### SceneWorks

SceneWorks is a Python-based API service (built with FastAPI) that manages a creative media production pipeline. It handles projects, characters, AI image generation jobs, model/LoRA manifest management, asset indexing, and person tracking — suggesting it is a backend for a generative AI scene/character production tool. The API coordinates between file-system project structures, a jobs store, and external worker processes to orchestrate AI-driven content generation.

