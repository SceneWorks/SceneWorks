# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-27 14:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a Python-based API service built on FastAPI that orchestrates AI-driven image and scene generation workflows. Its core responsibilities include: project and asset management (indexing and organizing generated images and related files), character and LoRA adapter model management (linking character definitions to fine-tuned model weights), job orchestration with worker lifecycle management (queuing and dispatching generation tasks to background workers), and real-time progress delivery via server-sent events (SSE). The system appears to act as a coordination layer between a user-facing interface and underlying AI image generation backends, managing the full pipeline from project setup through asset tracking. There is only one project component in this repository, so all functionality is self-contained within this single service. No cross-repository dependencies were identified, meaning external consumers or upstream providers are not directly observable from the codebase alone. The service likely depends on external AI inference infrastructure (e.g., diffusion model runtimes or LoRA-compatible generation engines) and possibly a database or file system for asset persistence, but these are inferred from architectural patterns rather than confirmed by explicit dependency declarations.

## Projects

### SceneWorks

SceneWorks is a Python-based API service (built with FastAPI) that manages AI-driven scene and image generation workflows. It handles project management, asset indexing, character/LoRA model management, job orchestration with worker lifecycle management, and server-sent events for real-time updates. The system appears to coordinate AI image generation pipelines with support for person tracking, model manifests, and character-linked LoRA adapters.

