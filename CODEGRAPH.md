# SceneWorks

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-24 22:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

SceneWorks is a FastAPI-based web API backend that orchestrates AI image and video generation workflows. It manages projects containing character assets, generative model manifests (including LoRA fine-tuned models), job queues for dispatching image generation tasks to worker processes, person-tracking data, and server-sent events for real-time status updates. The system appears to provide a pipeline where creative assets and characters are indexed, generative models are tracked and referenced via manifests, and generation jobs are created, dispatched to workers, and monitored. As a single-project repository, all components work together within this one service: the API layer handles client requests, the job queue layer coordinates with worker processes, and the manifest/asset management layer tracks the models and characters used in generation tasks. No cross-repository dependencies were identified, suggesting it operates as a self-contained backend service. External dependencies likely include a FastAPI runtime, an AI inference backend or worker pool (implied by job dispatch patterns), and potentially a database for project and job state persistence.

## Projects

### SceneWorks

SceneWorks is a web API backend (built with FastAPI based on the Request parameter patterns) that manages AI image/video generation workflows. It handles projects containing character assets, generative model manifests (including LoRA fine-tuned models), job queues for image generation workers, person-tracking data, and server-sent events for real-time updates. The project appears to orchestrate a pipeline where assets and characters are indexed, generative models are tracked via manifests, and image generation jobs are dispatched to and monitored across worker processes.

