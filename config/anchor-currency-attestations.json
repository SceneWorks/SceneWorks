{
  "_comment": "Memory-anchor CURRENCY ATTESTATIONS (sc-22667, epic sc-22657). An anchor's currency key is normally derived at its own measurement revision (scripts/anchor-loader-closure.mjs --stamp-anchors), so a pin bump that touches the model's loader closure stales it. This repository's invalidation doctrine is two-gated: a differing closure digest is not evidence that memory behaviour changed - only a load-or-device-path change without a behaviour witness is. An entry here records, per anchor, that the closure diff from `measuredRevision` to `attestedRevision` was read file by file and is accounting-only (contract pricing, byte walkers, facts, tests, docs) or is witnessed unchanged by a re-measure on the same hardware; `--stamp-anchors` then derives that anchor's key at `attestedRevision` and copies the attestation into the store's `source.currencyAttestation`. The attestation is BOUNDED: it names one revision, `measuredRevision` must still equal the cited record's inference revision, and a later pin bump that moves the loader closure past `attestedRevision` stales the anchor again until it is re-read (a new entry) or re-captured (delete the entry). Never write one for a load-or-device-path change without a measured witness - that is the false green the currency key exists to prevent. Reviewed narrative: docs/calibration/sc-22657/anchor-currency-attestation-sc-22667.md (c6d6a4db, sc-22667; extended to 8a65db2a by the sc-22723 feature pin, and to 563c44e1, 3b922bac, e16c6a55, e34d7b46 and e11fd9f0 by the sc-22738 terminal pins; the nine sc-22738 measured lower bounds on file at e16c6a55 are attested from 3b922bac by the same reading (two in PR #2782, seven in the campaign evidence PR that carries their records) - a bound is stamped by the same --stamp-anchors walk and held to the same two-gated doctrine, and its attestation claims only that the diff cannot change the peak the stop was reached at).",
  "attestations": [
    {
      "anchorId": "krea_2_turbo:candle:q4:base:base:krea-turbo-cuda-phase-curves-v1:imc-7f1a63e5459300aed47c",
      "measuredRevision": "3cd86ba2165f35db7c4fceecbeb7fbd12bca1c0c",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22723, sc-22738, sc-23026",
      "class": "witnessed-unchanged",
      "why": "3cd86ba2..c6d6a4db DOES touch the Krea loader path (candle-gen-krea loader.rs / pipeline.rs / transformer/mod.rs / control*.rs, candle-gen quant/mod.rs + quant/adapt.rs, candle-gen-boogu, candle-gen-qwen-image and candle-gen-pid files the closure reaches), so the diff alone cannot classify this anchor. It is attested on a MEASURED witness instead: the E6 falsification fixture re-measured krea_2_turbo q4 at a5f643ae (22 first-parent merges after 3cd86ba2) on the same GPU and the cold staged control reproduces the packaged anchor per phase. The remaining a5f643ae..c6d6a4db range is accounting-only: gen-core lib.rs (re-export), memory_strategy.rs (contract types), weightsmeta.rs (walker hidden-cache skip) and candle-gen quant/sidecar.rs (CACHE_DIR constant re-homed, same value). Extended to 8a65db2a (sc-22723 feature pin, inference PRs #946-#958): c6d6a4db..8a65db2a touches four files of this closure and none is a load or device path - gen-core encoder_contract.rs (packed-marker struct kept whole, a stale-marker allowance only mlx-gen-flux2 calls, the hf_cache_discovery_roots authorization helper, tests), gen-core lib.rs (its re-export), gen-core wan_i2v_memory.rs (Wan-only identity tokens) and candle-gen-krea lib.rs (the per-(route, proven tier) identity table and weights-free surfaces; the q4 turbo identity this anchor is keyed to is unchanged, and the new provable-tier read consults the marker the loader already validated). loader.rs, pipeline.rs, transformer/, control*.rs, candle-gen quant/ and every device path are untouched. Extended to 563c44e1 (sc-22738, the epic final pin = inference main, merge of feature PR #960 over main 1cd0e393): 8a65db2a..563c44e1 changes nine inference files and exactly two are in this closure, gen-core encoder_contract.rs and lib.rs. Both are a REMOVAL, not a change of behaviour: inference PR #961 (sync of main into the feature branch) drops the sc-22727 stale-packed-marker tolerance that sc-22760 made unnecessary - the two entry points `source_for_load_with_stale_packed_marker` / `validate_source_for_discovery_with_stale_packed_marker`, the `RequiredWithStalePackedMarker` policy arm and its `stale_packed_marker()` reader, the `declared_quant` rewrite in `validate_source_with_policy` that was the identity for every other policy, `PackedQuantization` back to crate-private, and the allowance test; lib.rs drops the matching re-export. The only production caller of the removed surface was mlx-gen-flux2 (the FLUX.2 Klein turnkey inventory), which this closure does not reach; `source_for_load`, `validate_source_for_discovery`, `requires_config` for `Required` and every shard/header/quantization-evidence check this model executes are byte-for-byte the same code path before and after. The remaining seven files (mlx-gen-flux2 artifact_inventory.rs / loader.rs / memory_strategy.rs / model.rs, its two test files, release/real-weight-models.toml) are outside this closure. No load or device path this anchor reaches changed. Extended to 3b922bac (sc-22738, inference main = the merge of PR #962 over 563c44e1, its first parent): 563c44e1..3b922bac changes fourteen inference files, all under crates/media/mlx-gen/mlx-gen-krea and crates/media/mlx-gen/mlx-gen-krea-realtime (the harness-authorized calibration_fault hooks at every phase exit of the MLX krea base and realtime generators, their memory_strategy.rs helpers and tests), and the intersection with this closure's closureFiles is EMPTY - this is the CANDLE krea closure (candle-gen-krea and candle-gen), which never links mlx-gen-krea or mlx-gen-krea-realtime. The closure digest is byte-identical across the range (config/anchor-loader-closures.json records the same digest for this model at 563c44e1 and 3b922bac; only krea_2_raw:mlx and krea_2_turbo:mlx moved), so this is the trivial extension the bump script's remediation names: nothing this anchor loads or executes changed, and no file is added to filesChangedSinceMeasurement. Extended to e16c6a55 (sc-22738 fourth terminal pin, 2026-09-07; inference main = the merge of PR #965 fix/sc-22738-wan-rehost-tolerance over #964 fix/sc-22738-wan-t2v-memory-contract over #963 fix/sc-22738-mlx-progress-boundaries over 3b922bac): 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test) and the intersection with this closure's closureFiles is exactly ONE file, crates/contracts/gen-core/src/residency.rs. That diff is instrumentation-only (+195/-8, of which 147 lines are new unit tests): ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the two warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) AFTER the prompt encode, both gated on the new warm_needs_load; the staged arms, run_two_phase, the components loaded, their order, their devices and the offload policy are byte-identical. This anchor was measured on the STAGED arm (measuredRegime.staged = true: the 3cd86ba2 capture drove candle-gen-krea's run_request_scoped with stage_residency), which the diff does not touch, so neither what it loads nor where the phase boundaries it was cut on fall changed. No file is added to filesChangedSinceMeasurement. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (143 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (143 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "docs/calibration/sc-22657/candle-five-rung-falsification-sc-22667.json krea_2_turbo q4 coldStagedControl at a5f643ae against the packaged anchor: conditioning 3.694 GB vs 3.828 (-3.5%, under), denoise 15.099 vs 15.103 (-0.02%), decode 22.349 vs 22.352 (-0.01%). The batch's resident rung (12.73 GB conditioning) also brackets the contract's 11.73 GB component total, as the E6 test asserts. No re-measure for c6d6a4db..8a65db2a: that segment carries no load or device path change (files listed), so the classification stands on the diff, as the doctrine allows.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/caption.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/duration_head.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/encoder_contract.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: ValidatedEncoderSource keeps the whole PackedQuantization marker instead of its bits with same-value accessors; a stale-packed-marker allowance policy plus two entry points (`source_for_load_with_stale_packed_marker`, `validate_source_for_discovery_with_stale_packed_marker`) that only mlx-gen-flux2 calls; `hf_cache_discovery_roots`, a source-authorization helper that names the Hugging Face cache repository dir as a permitted symlink target; tests); 8a65db2a..563c44e1: removal of the stale-packed-marker allowance (two entry points, one policy arm, an identity match in validate_source_with_policy, PackedQuantization back to private, its test) whose only caller was mlx-gen-flux2 - not on this closure's load path"
        },
        {
          "path": "crates/contracts/gen-core/src/exr_io.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/gemma_assets.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/generator.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/hdr.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/imageops.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/lib.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: re-export of `hf_cache_discovery_roots`); 8a65db2a..563c44e1: the matching PackedQuantization re-export removed"
        },
        {
          "path": "crates/contracts/gen-core/src/ltx_checkpoint.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/ltx_dfr.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/media.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/contracts/gen-core/src/registry.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/runtime.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/sd3_encoder_artifacts.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/sd3_request.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/tokenizer.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/train.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: Wan A14B route/backend/tier calibration-identity tokens and the T2V-14B route enum; Wan-only contract accounting, never executed by this model)"
        },
        {
          "path": "crates/contracts/gen-core/src/weightsmeta.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-boogu/src/lib.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-boogu/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-boogu/src/pipeline.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-boogu/src/tokenizer.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-boogu/src/vision/preprocess.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/control.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/control_provider.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/lib.rs",
          "class": "accounting-only for c6d6a4db..8a65db2a (per-(route, proven tier) calibration-identity table; the q4 turbo string `krea-turbo-cuda-phase-curves-v1` this anchor is keyed to is unchanged; weights-free registry-behavior surfaces; `krea_provable_artifact_tier` reads the marker the existing `validate_load_spec`/`actual_quant_tier` path already consults; tests). The earlier 3cd86ba2..a5f643ae range is covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/loader.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/pipeline.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/train_dit.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/training.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/transformer/mod.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-krea/src/vision.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/backbone/mod.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/backbone/rope.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/caption.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/engine.rs",
          "class": "same-value-constant"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-qwen-image/src/lib.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-qwen-image/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-qwen-image/src/vae.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/architecture_facts.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/diffusion_schedule.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/lib.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/adapt.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/mod.rs",
          "class": "not read individually - covered by the measured witness"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/sidecar.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/train/flow_match.rs",
          "class": "not read individually - covered by the measured witness"
        }
      ]
    },
    {
      "anchorId": "z_image_turbo:candle:bf16:base:base:z-image-cuda-staged-tiled-decode-bounded-attention-device-format-blocks-v2:imc-3cd69f2fee9e91e83b58",
      "measuredRevision": "670dc1f4418859cf241471987a0a050fedc108c5",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22723, sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "Every file the z_image_turbo:candle loader closure changed in 670dc1f4..c6d6a4db carries contract ACCOUNTING, not load or device behaviour: candle-gen-z-image/src/memory_strategy.rs grew the per-component asset facts, architecture facts and loaded-path pricing the sc-22657 law reads; candle-gen/src/architecture_facts.rs is a new snapshot-config reader for those facts; gen-core's memory_strategy.rs / weightsmeta.rs / wan_i2v_memory.rs carry the contract types and the tensor-free byte walkers (materialized_* readers; the .candle-device-format-v1 hidden-cache skip) that price the checkpoint without touching the loader; gen-core lib.rs and candle-gen lib.rs are re-export / `pub mod` lines for those; gen-core registry.rs and runtime.rs move a #[cfg(test)] helper and fix a Windows-only test fixture; candle-gen-pid/src/engine.rs names the f32 load dtype and merged-file constants it already used (same values, and PiD is not on the Z-Image path); candle-gen/src/quant/sidecar.rs re-homes the `.candle-device-format-v1` CACHE_DIR literal to gen-core's constant with the same value. No loader, pipeline, weights, kernel, quant or device-format code path changed. Extended to 8a65db2a (sc-22723 feature pin, inference PRs #946-#958): c6d6a4db..8a65db2a touches four files of this closure and none is a load or device path - gen-core encoder_contract.rs (packed-marker struct kept whole with same-value accessors, a stale-marker allowance only mlx-gen-flux2 calls, the hf_cache_discovery_roots authorization helper, tests), gen-core lib.rs (its re-export), gen-core wan_i2v_memory.rs (Wan-only identity tokens) and candle-gen-z-image memory_strategy.rs, whose selected_encoder_discovery_roots now authorizes the HF-cache repository dir as a symlink target for the text-encoder planning-facts readers - a validation gate, not a load: the same bytes reach the same device the same way. pipeline.rs, model.rs, the loader and every device-format path are untouched. Extended to 563c44e1 (sc-22738, the epic final pin = inference main, merge of feature PR #960 over main 1cd0e393): 8a65db2a..563c44e1 changes nine inference files and exactly two are in this closure, gen-core encoder_contract.rs and lib.rs. Both are a REMOVAL, not a change of behaviour: inference PR #961 (sync of main into the feature branch) drops the sc-22727 stale-packed-marker tolerance that sc-22760 made unnecessary - the two entry points `source_for_load_with_stale_packed_marker` / `validate_source_for_discovery_with_stale_packed_marker`, the `RequiredWithStalePackedMarker` policy arm and its `stale_packed_marker()` reader, the `declared_quant` rewrite in `validate_source_with_policy` that was the identity for every other policy, `PackedQuantization` back to crate-private, and the allowance test; lib.rs drops the matching re-export. The only production caller of the removed surface was mlx-gen-flux2 (the FLUX.2 Klein turnkey inventory), which this closure does not reach; `source_for_load`, `validate_source_for_discovery`, `requires_config` for `Required` and every shard/header/quantization-evidence check this model executes are byte-for-byte the same code path before and after. The remaining seven files (mlx-gen-flux2 artifact_inventory.rs / loader.rs / memory_strategy.rs / model.rs, its two test files, release/real-weight-models.toml) are outside this closure. No load or device path this anchor reaches changed. Extended to 3b922bac (sc-22738, inference main = the merge of PR #962 over 563c44e1, its first parent): 563c44e1..3b922bac changes fourteen inference files, all under crates/media/mlx-gen/mlx-gen-krea and crates/media/mlx-gen/mlx-gen-krea-realtime (the harness-authorized calibration_fault hooks at every phase exit of the MLX krea base and realtime generators, their memory_strategy.rs helpers and tests), and the intersection with this closure's closureFiles is EMPTY - the Z-Image candle closure (candle-gen-z-image) never links either krea crate. The closure digest is byte-identical across the range (config/anchor-loader-closures.json records the same digest for this model at 563c44e1 and 3b922bac; only krea_2_raw:mlx and krea_2_turbo:mlx moved), so this is the trivial extension the bump script's remediation names: nothing this anchor loads or executes changed, and no file is added to filesChangedSinceMeasurement. Extended to e16c6a55 (sc-22738 fourth terminal pin, 2026-09-07; inference main = the merge of PR #965 fix/sc-22738-wan-rehost-tolerance over #964 fix/sc-22738-wan-t2v-memory-contract over #963 fix/sc-22738-mlx-progress-boundaries over 3b922bac): 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test) and the intersection with this closure's closureFiles is exactly ONE file, crates/contracts/gen-core/src/residency.rs. That diff is instrumentation-only (+195/-8, of which 147 lines are new unit tests): ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the two warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) AFTER the prompt encode, both gated on the new warm_needs_load; the staged arms, run_two_phase, the components loaded, their order, their devices and the offload policy are byte-identical. candle-gen-z-image links gen-core but never drives gen_core::residency::Residency (the crate has no run_request_scoped, run_staged_request_scoped or run_two_phase call); residency.rs is in this closure only as a gen-core module and is unreached by the Z-Image Candle load and render path. No file is added to filesChangedSinceMeasurement. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (109 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (109 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "No hardware re-measure of the bf16 tier itself; the classification stands on the diff alone, which is the same file set as the q4 anchor's (the closure is per model, not per tier) and carries no load or device path change. The q4 tier's cold-staged re-measure at a5f643ae (docs/calibration/sc-22657/candle-five-rung-falsification-sc-22667.json) witnesses that the shared loader's memory behaviour did not move across the larger half of this range. No re-measure for c6d6a4db..8a65db2a: that segment carries no load or device path change (files listed), so the classification stands on the diff, as the doctrine allows.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/encoder_contract.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: ValidatedEncoderSource keeps the whole PackedQuantization marker instead of its bits with same-value accessors; a stale-packed-marker allowance policy plus two entry points (`source_for_load_with_stale_packed_marker`, `validate_source_for_discovery_with_stale_packed_marker`) that only mlx-gen-flux2 calls; `hf_cache_discovery_roots`, a source-authorization helper that names the Hugging Face cache repository dir as a permitted symlink target; tests); 8a65db2a..563c44e1: removal of the stale-packed-marker allowance (two entry points, one policy arm, an identity match in validate_source_with_policy, PackedQuantization back to private, its test) whose only caller was mlx-gen-flux2 - not on this closure's load path"
        },
        {
          "path": "crates/contracts/gen-core/src/lib.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: re-export of `hf_cache_discovery_roots`); 8a65db2a..563c44e1: the matching PackedQuantization re-export removed"
        },
        {
          "path": "crates/contracts/gen-core/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/contracts/gen-core/src/registry.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/runtime.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: Wan A14B route/backend/tier calibration-identity tokens and the T2V-14B route enum; Wan-only contract accounting, never executed by this model)"
        },
        {
          "path": "crates/contracts/gen-core/src/weightsmeta.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/engine.rs",
          "class": "same-value-constant"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-z-image/src/memory_strategy.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: `selected_encoder_discovery_roots` delegates to `gen_core::hf_cache_discovery_roots`, widening the text-encoder discovery confinement to the HF-cache repository dir - a source-authorization gate consumed only by the planning-facts readers in this file, allocating and materializing nothing; plus a test)"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/architecture_facts.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/lib.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/sidecar.rs",
          "class": "accounting-only"
        }
      ]
    },
    {
      "anchorId": "z_image_turbo:candle:q4:base:base:z-image-cuda-staged-tiled-decode-bounded-attention-device-format-blocks-v2:imc-b8064416c974b5008509",
      "measuredRevision": "670dc1f4418859cf241471987a0a050fedc108c5",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22723, sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "Every file the z_image_turbo:candle loader closure changed in 670dc1f4..c6d6a4db carries contract ACCOUNTING, not load or device behaviour: candle-gen-z-image/src/memory_strategy.rs grew the per-component asset facts, architecture facts and loaded-path pricing the sc-22657 law reads; candle-gen/src/architecture_facts.rs is a new snapshot-config reader for those facts; gen-core's memory_strategy.rs / weightsmeta.rs / wan_i2v_memory.rs carry the contract types and the tensor-free byte walkers (materialized_* readers; the .candle-device-format-v1 hidden-cache skip) that price the checkpoint without touching the loader; gen-core lib.rs and candle-gen lib.rs are re-export / `pub mod` lines for those; gen-core registry.rs and runtime.rs move a #[cfg(test)] helper and fix a Windows-only test fixture; candle-gen-pid/src/engine.rs names the f32 load dtype and merged-file constants it already used (same values, and PiD is not on the Z-Image path); candle-gen/src/quant/sidecar.rs re-homes the `.candle-device-format-v1` CACHE_DIR literal to gen-core's constant with the same value. No loader, pipeline, weights, kernel, quant or device-format code path changed. Extended to 8a65db2a (sc-22723 feature pin, inference PRs #946-#958): c6d6a4db..8a65db2a touches four files of this closure and none is a load or device path - gen-core encoder_contract.rs (packed-marker struct kept whole with same-value accessors, a stale-marker allowance only mlx-gen-flux2 calls, the hf_cache_discovery_roots authorization helper, tests), gen-core lib.rs (its re-export), gen-core wan_i2v_memory.rs (Wan-only identity tokens) and candle-gen-z-image memory_strategy.rs, whose selected_encoder_discovery_roots now authorizes the HF-cache repository dir as a symlink target for the text-encoder planning-facts readers - a validation gate, not a load: the same bytes reach the same device the same way. pipeline.rs, model.rs, the loader and every device-format path are untouched. Extended to 563c44e1 (sc-22738, the epic final pin = inference main, merge of feature PR #960 over main 1cd0e393): 8a65db2a..563c44e1 changes nine inference files and exactly two are in this closure, gen-core encoder_contract.rs and lib.rs. Both are a REMOVAL, not a change of behaviour: inference PR #961 (sync of main into the feature branch) drops the sc-22727 stale-packed-marker tolerance that sc-22760 made unnecessary - the two entry points `source_for_load_with_stale_packed_marker` / `validate_source_for_discovery_with_stale_packed_marker`, the `RequiredWithStalePackedMarker` policy arm and its `stale_packed_marker()` reader, the `declared_quant` rewrite in `validate_source_with_policy` that was the identity for every other policy, `PackedQuantization` back to crate-private, and the allowance test; lib.rs drops the matching re-export. The only production caller of the removed surface was mlx-gen-flux2 (the FLUX.2 Klein turnkey inventory), which this closure does not reach; `source_for_load`, `validate_source_for_discovery`, `requires_config` for `Required` and every shard/header/quantization-evidence check this model executes are byte-for-byte the same code path before and after. The remaining seven files (mlx-gen-flux2 artifact_inventory.rs / loader.rs / memory_strategy.rs / model.rs, its two test files, release/real-weight-models.toml) are outside this closure. No load or device path this anchor reaches changed. Extended to 3b922bac (sc-22738, inference main = the merge of PR #962 over 563c44e1, its first parent): 563c44e1..3b922bac changes fourteen inference files, all under crates/media/mlx-gen/mlx-gen-krea and crates/media/mlx-gen/mlx-gen-krea-realtime (the harness-authorized calibration_fault hooks at every phase exit of the MLX krea base and realtime generators, their memory_strategy.rs helpers and tests), and the intersection with this closure's closureFiles is EMPTY - the Z-Image candle closure (candle-gen-z-image) never links either krea crate. The closure digest is byte-identical across the range (config/anchor-loader-closures.json records the same digest for this model at 563c44e1 and 3b922bac; only krea_2_raw:mlx and krea_2_turbo:mlx moved), so this is the trivial extension the bump script's remediation names: nothing this anchor loads or executes changed, and no file is added to filesChangedSinceMeasurement. Extended to e16c6a55 (sc-22738 fourth terminal pin, 2026-09-07; inference main = the merge of PR #965 fix/sc-22738-wan-rehost-tolerance over #964 fix/sc-22738-wan-t2v-memory-contract over #963 fix/sc-22738-mlx-progress-boundaries over 3b922bac): 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test) and the intersection with this closure's closureFiles is exactly ONE file, crates/contracts/gen-core/src/residency.rs. That diff is instrumentation-only (+195/-8, of which 147 lines are new unit tests): ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the two warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) AFTER the prompt encode, both gated on the new warm_needs_load; the staged arms, run_two_phase, the components loaded, their order, their devices and the offload policy are byte-identical. candle-gen-z-image links gen-core but never drives gen_core::residency::Residency (the crate has no run_request_scoped, run_staged_request_scoped or run_two_phase call); residency.rs is in this closure only as a gen-core module and is unreached by the Z-Image Candle load and render path. No file is added to filesChangedSinceMeasurement. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (109 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (109 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "docs/calibration/sc-22657/candle-five-rung-falsification-sc-22667.json: the same tier re-measured at a5f643ae on the same GPU (memory-candle-adapter run_batch, coldStagedControl) lands on the packaged anchor within the lane's recapture spread - denoise +0.07%, decode +0.04%, conditioning -3.5% (the phase's own cold-start jitter, under the anchor). a5f643ae..c6d6a4db then touches only gen-core lib.rs / memory_strategy.rs / weightsmeta.rs, candle-gen quant/sidecar.rs (constant re-home) and candle-gen-z-image memory_strategy.rs. No re-measure for c6d6a4db..8a65db2a: that segment carries no load or device path change (files listed), so the classification stands on the diff, as the doctrine allows.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/encoder_contract.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: ValidatedEncoderSource keeps the whole PackedQuantization marker instead of its bits with same-value accessors; a stale-packed-marker allowance policy plus two entry points (`source_for_load_with_stale_packed_marker`, `validate_source_for_discovery_with_stale_packed_marker`) that only mlx-gen-flux2 calls; `hf_cache_discovery_roots`, a source-authorization helper that names the Hugging Face cache repository dir as a permitted symlink target; tests); 8a65db2a..563c44e1: removal of the stale-packed-marker allowance (two entry points, one policy arm, an identity match in validate_source_with_policy, PackedQuantization back to private, its test) whose only caller was mlx-gen-flux2 - not on this closure's load path"
        },
        {
          "path": "crates/contracts/gen-core/src/lib.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: re-export of `hf_cache_discovery_roots`); 8a65db2a..563c44e1: the matching PackedQuantization re-export removed"
        },
        {
          "path": "crates/contracts/gen-core/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/contracts/gen-core/src/registry.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/runtime.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: Wan A14B route/backend/tier calibration-identity tokens and the T2V-14B route enum; Wan-only contract accounting, never executed by this model)"
        },
        {
          "path": "crates/contracts/gen-core/src/weightsmeta.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/engine.rs",
          "class": "same-value-constant"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-z-image/src/memory_strategy.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: `selected_encoder_discovery_roots` delegates to `gen_core::hf_cache_discovery_roots`, widening the text-encoder discovery confinement to the HF-cache repository dir - a source-authorization gate consumed only by the planning-facts readers in this file, allocating and materializing nothing; plus a test)"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/architecture_facts.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/lib.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/sidecar.rs",
          "class": "accounting-only"
        }
      ]
    },
    {
      "anchorId": "z_image_turbo:candle:q8:base:base:z-image-cuda-staged-tiled-decode-bounded-attention-device-format-blocks-v2:imc-8cda4f70f78dd33f5330",
      "measuredRevision": "670dc1f4418859cf241471987a0a050fedc108c5",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22723, sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "Every file the z_image_turbo:candle loader closure changed in 670dc1f4..c6d6a4db carries contract ACCOUNTING, not load or device behaviour: candle-gen-z-image/src/memory_strategy.rs grew the per-component asset facts, architecture facts and loaded-path pricing the sc-22657 law reads; candle-gen/src/architecture_facts.rs is a new snapshot-config reader for those facts; gen-core's memory_strategy.rs / weightsmeta.rs / wan_i2v_memory.rs carry the contract types and the tensor-free byte walkers (materialized_* readers; the .candle-device-format-v1 hidden-cache skip) that price the checkpoint without touching the loader; gen-core lib.rs and candle-gen lib.rs are re-export / `pub mod` lines for those; gen-core registry.rs and runtime.rs move a #[cfg(test)] helper and fix a Windows-only test fixture; candle-gen-pid/src/engine.rs names the f32 load dtype and merged-file constants it already used (same values, and PiD is not on the Z-Image path); candle-gen/src/quant/sidecar.rs re-homes the `.candle-device-format-v1` CACHE_DIR literal to gen-core's constant with the same value. No loader, pipeline, weights, kernel, quant or device-format code path changed. Extended to 8a65db2a (sc-22723 feature pin, inference PRs #946-#958): c6d6a4db..8a65db2a touches four files of this closure and none is a load or device path - gen-core encoder_contract.rs (packed-marker struct kept whole with same-value accessors, a stale-marker allowance only mlx-gen-flux2 calls, the hf_cache_discovery_roots authorization helper, tests), gen-core lib.rs (its re-export), gen-core wan_i2v_memory.rs (Wan-only identity tokens) and candle-gen-z-image memory_strategy.rs, whose selected_encoder_discovery_roots now authorizes the HF-cache repository dir as a symlink target for the text-encoder planning-facts readers - a validation gate, not a load: the same bytes reach the same device the same way. pipeline.rs, model.rs, the loader and every device-format path are untouched. Extended to 563c44e1 (sc-22738, the epic final pin = inference main, merge of feature PR #960 over main 1cd0e393): 8a65db2a..563c44e1 changes nine inference files and exactly two are in this closure, gen-core encoder_contract.rs and lib.rs. Both are a REMOVAL, not a change of behaviour: inference PR #961 (sync of main into the feature branch) drops the sc-22727 stale-packed-marker tolerance that sc-22760 made unnecessary - the two entry points `source_for_load_with_stale_packed_marker` / `validate_source_for_discovery_with_stale_packed_marker`, the `RequiredWithStalePackedMarker` policy arm and its `stale_packed_marker()` reader, the `declared_quant` rewrite in `validate_source_with_policy` that was the identity for every other policy, `PackedQuantization` back to crate-private, and the allowance test; lib.rs drops the matching re-export. The only production caller of the removed surface was mlx-gen-flux2 (the FLUX.2 Klein turnkey inventory), which this closure does not reach; `source_for_load`, `validate_source_for_discovery`, `requires_config` for `Required` and every shard/header/quantization-evidence check this model executes are byte-for-byte the same code path before and after. The remaining seven files (mlx-gen-flux2 artifact_inventory.rs / loader.rs / memory_strategy.rs / model.rs, its two test files, release/real-weight-models.toml) are outside this closure. No load or device path this anchor reaches changed. Extended to 3b922bac (sc-22738, inference main = the merge of PR #962 over 563c44e1, its first parent): 563c44e1..3b922bac changes fourteen inference files, all under crates/media/mlx-gen/mlx-gen-krea and crates/media/mlx-gen/mlx-gen-krea-realtime (the harness-authorized calibration_fault hooks at every phase exit of the MLX krea base and realtime generators, their memory_strategy.rs helpers and tests), and the intersection with this closure's closureFiles is EMPTY - the Z-Image candle closure (candle-gen-z-image) never links either krea crate. The closure digest is byte-identical across the range (config/anchor-loader-closures.json records the same digest for this model at 563c44e1 and 3b922bac; only krea_2_raw:mlx and krea_2_turbo:mlx moved), so this is the trivial extension the bump script's remediation names: nothing this anchor loads or executes changed, and no file is added to filesChangedSinceMeasurement. Extended to e16c6a55 (sc-22738 fourth terminal pin, 2026-09-07; inference main = the merge of PR #965 fix/sc-22738-wan-rehost-tolerance over #964 fix/sc-22738-wan-t2v-memory-contract over #963 fix/sc-22738-mlx-progress-boundaries over 3b922bac): 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test) and the intersection with this closure's closureFiles is exactly ONE file, crates/contracts/gen-core/src/residency.rs. That diff is instrumentation-only (+195/-8, of which 147 lines are new unit tests): ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the two warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) AFTER the prompt encode, both gated on the new warm_needs_load; the staged arms, run_two_phase, the components loaded, their order, their devices and the offload policy are byte-identical. candle-gen-z-image links gen-core but never drives gen_core::residency::Residency (the crate has no run_request_scoped, run_staged_request_scoped or run_two_phase call); residency.rs is in this closure only as a gen-core module and is unreached by the Z-Image Candle load and render path. No file is added to filesChangedSinceMeasurement. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (109 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (109 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "No hardware re-measure of the q8 tier itself; the classification stands on the diff alone, which is the same file set as the q4 anchor's (the closure is per model, not per tier) and carries no load or device path change. The q4 tier's cold-staged re-measure at a5f643ae (docs/calibration/sc-22657/candle-five-rung-falsification-sc-22667.json) witnesses that the shared loader's memory behaviour did not move across the larger half of this range. No re-measure for c6d6a4db..8a65db2a: that segment carries no load or device path change (files listed), so the classification stands on the diff, as the doctrine allows.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/encoder_contract.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: ValidatedEncoderSource keeps the whole PackedQuantization marker instead of its bits with same-value accessors; a stale-packed-marker allowance policy plus two entry points (`source_for_load_with_stale_packed_marker`, `validate_source_for_discovery_with_stale_packed_marker`) that only mlx-gen-flux2 calls; `hf_cache_discovery_roots`, a source-authorization helper that names the Hugging Face cache repository dir as a permitted symlink target; tests); 8a65db2a..563c44e1: removal of the stale-packed-marker allowance (two entry points, one policy arm, an identity match in validate_source_with_policy, PackedQuantization back to private, its test) whose only caller was mlx-gen-flux2 - not on this closure's load path"
        },
        {
          "path": "crates/contracts/gen-core/src/lib.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: re-export of `hf_cache_discovery_roots`); 8a65db2a..563c44e1: the matching PackedQuantization re-export removed"
        },
        {
          "path": "crates/contracts/gen-core/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/contracts/gen-core/src/registry.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/runtime.rs",
          "class": "test-only"
        },
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: Wan A14B route/backend/tier calibration-identity tokens and the T2V-14B route enum; Wan-only contract accounting, never executed by this model)"
        },
        {
          "path": "crates/contracts/gen-core/src/weightsmeta.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-pid/src/engine.rs",
          "class": "same-value-constant"
        },
        {
          "path": "crates/media/candle-gen/candle-gen-z-image/src/memory_strategy.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: `selected_encoder_discovery_roots` delegates to `gen_core::hf_cache_discovery_roots`, widening the text-encoder discovery confinement to the HF-cache repository dir - a source-authorization gate consumed only by the planning-facts readers in this file, allocating and materializing nothing; plus a test)"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/architecture_facts.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/lib.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/candle-gen/candle-gen/src/quant/sidecar.rs",
          "class": "accounting-only"
        }
      ]
    },
    {
      "anchorId": "z_image_turbo:mlx:q4:base:base:z-image-mlx-independent-materialization-v4:imc-2284d75c08c2a13f1b7e",
      "measuredRevision": "a5f643ae58a4ed81e6be8280afbf29750da5ffe2",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22723, sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "a5f643ae..c6d6a4db changes four files in the z_image_turbo:mlx closure and none is a load or device path: gen-core lib.rs adds the CANDLE_DEVICE_FORMAT_CACHE_DIR re-export; gen-core memory_strategy.rs carries contract types; gen-core weightsmeta.rs makes the tensor-free byte walkers skip hidden directories (a pricing read, never a tensor load - and MLX never writes that cache); mlx-gen-z-image memory_strategy.rs re-prices the contract's asset facts. mlx-gen-z-image model.rs, the loader, and mlx-gen/src are untouched. Extended to 8a65db2a (sc-22723 feature pin, inference PRs #946-#958): c6d6a4db..8a65db2a touches four files of this closure and none is a load or device path - core-llm starvector.rs (a StarVector decode progress event the Z-Image encoder never executes), gen-core encoder_contract.rs (packed-marker struct kept whole with same-value accessors, a stale-marker allowance only mlx-gen-flux2 calls, the hf_cache_discovery_roots helper, tests), gen-core lib.rs (its re-export) and gen-core wan_i2v_memory.rs (Wan-only identity tokens). mlx-gen-z-image, mlx-gen-pid and mlx-gen/src are untouched. Extended to 563c44e1 (sc-22738, the epic final pin = inference main, merge of feature PR #960 over main 1cd0e393): 8a65db2a..563c44e1 changes nine inference files and exactly two are in this closure, gen-core encoder_contract.rs and lib.rs. Both are a REMOVAL, not a change of behaviour: inference PR #961 (sync of main into the feature branch) drops the sc-22727 stale-packed-marker tolerance that sc-22760 made unnecessary - the two entry points `source_for_load_with_stale_packed_marker` / `validate_source_for_discovery_with_stale_packed_marker`, the `RequiredWithStalePackedMarker` policy arm and its `stale_packed_marker()` reader, the `declared_quant` rewrite in `validate_source_with_policy` that was the identity for every other policy, `PackedQuantization` back to crate-private, and the allowance test; lib.rs drops the matching re-export. The only production caller of the removed surface was mlx-gen-flux2 (the FLUX.2 Klein turnkey inventory), which this closure does not reach; `source_for_load`, `validate_source_for_discovery`, `requires_config` for `Required` and every shard/header/quantization-evidence check this model executes are byte-for-byte the same code path before and after. The remaining seven files (mlx-gen-flux2 artifact_inventory.rs / loader.rs / memory_strategy.rs / model.rs, its two test files, release/real-weight-models.toml) are outside this closure. No load or device path this anchor reaches changed. Extended to 3b922bac (sc-22738, inference main = the merge of PR #962 over 563c44e1, its first parent): 563c44e1..3b922bac changes fourteen inference files, all under crates/media/mlx-gen/mlx-gen-krea and crates/media/mlx-gen/mlx-gen-krea-realtime (the harness-authorized calibration_fault hooks at every phase exit of the MLX krea base and realtime generators, their memory_strategy.rs helpers and tests), and the intersection with this closure's closureFiles is EMPTY - the Z-Image MLX closure (mlx-gen-z-image, mlx-gen-pid, mlx-gen/src) never links mlx-gen-krea or mlx-gen-krea-realtime. The closure digest is byte-identical across the range (config/anchor-loader-closures.json records the same digest for this model at 563c44e1 and 3b922bac; only krea_2_raw:mlx and krea_2_turbo:mlx moved), so this is the trivial extension the bump script's remediation names: nothing this anchor loads or executes changed, and no file is added to filesChangedSinceMeasurement. Extended to e16c6a55 (sc-22738 fourth terminal pin, 2026-09-07; inference main = the merge of PR #965 fix/sc-22738-wan-rehost-tolerance over #964 fix/sc-22738-wan-t2v-memory-contract over #963 fix/sc-22738-mlx-progress-boundaries over 3b922bac): 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test) and the intersection with this closure's closureFiles is exactly ONE file, crates/contracts/gen-core/src/residency.rs. That diff is instrumentation-only (+195/-8, of which 147 lines are new unit tests): ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the two warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) AFTER the prompt encode, both gated on the new warm_needs_load; the staged arms, run_two_phase, the components loaded, their order, their devices and the offload policy are byte-identical. mlx-gen-z-image drives run_staged_request_scoped and this anchor was measured on the WARM arm (measuredRegime.staged = false), so the moved Loading(Renderer) IS on its path - but the loads it triggers are unchanged, and the MLX adapter's Z-Image capture (memory-mlx-adapter run_z_image_reference_loaded) cuts conditioning on the first Progress::Step and denoise on Progress::Decoding, neither of which residency.rs emits or moved, so both the memory behaviour and the phase attribution the packaged record was minted under are unaffected. No file is added to filesChangedSinceMeasurement. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (137 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (137 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "The anchor itself was captured at a5f643ae on the nax-macos runner (macos-mlx.yml run_five_rung_reference, run 33817079839), i.e. after every loader-path change in the store's history; the range to the pin is the accounting diff listed here and carries nothing the MLX loader executes. No re-measure for c6d6a4db..8a65db2a: that segment carries no load or device path change (files listed), so the classification stands on the diff, as the doctrine allows.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/core-llm/src/starvector.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: a StarVector decode `Progress { generated_tokens }` event variant and accessor; the Z-Image text encoder never executes the StarVector module)"
        },
        {
          "path": "crates/contracts/gen-core/src/encoder_contract.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: ValidatedEncoderSource keeps the whole PackedQuantization marker instead of its bits with same-value accessors; a stale-packed-marker allowance policy plus two entry points (`source_for_load_with_stale_packed_marker`, `validate_source_for_discovery_with_stale_packed_marker`) that only mlx-gen-flux2 calls; `hf_cache_discovery_roots`, a source-authorization helper that names the Hugging Face cache repository dir as a permitted symlink target; tests); 8a65db2a..563c44e1: removal of the stale-packed-marker allowance (two entry points, one policy arm, an identity match in validate_source_with_policy, PackedQuantization back to private, its test) whose only caller was mlx-gen-flux2 - not on this closure's load path"
        },
        {
          "path": "crates/contracts/gen-core/src/lib.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: re-export of `hf_cache_discovery_roots`); 8a65db2a..563c44e1: the matching PackedQuantization re-export removed"
        },
        {
          "path": "crates/contracts/gen-core/src/memory_strategy.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (c6d6a4db..8a65db2a: Wan A14B route/backend/tier calibration-identity tokens and the T2V-14B route enum; Wan-only contract accounting, never executed by this model)"
        },
        {
          "path": "crates/contracts/gen-core/src/weightsmeta.rs",
          "class": "accounting-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-z-image/src/memory_strategy.rs",
          "class": "accounting-only"
        }
      ]
    },
    {
      "anchorId": "exceeded:bernini:mlx:bf16:base:base:bernini-image-bf16-mlx-dual-expert-ladder-v1:exc-15a6249c248c324c0fab",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): bernini:bf16:mlx at 848x480x49 was stopped by the footprint watchdog at 97,147,294,328 bytes on a 137,438,953,472-byte host at 3b922bac, mid z16 VAE decode. The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files; the intersection with this closure (bernini:mlx, 117 files) is six, read one by one. mlx-gen-bernini/src/bernini.rs is the generator body this bound was measured in and is the only executed file that changed: generate_impl gains on_progress(Progress::Loading(LoadPhase::TextEncoder)) before the planner load and on_progress(Progress::Loading(LoadPhase::Renderer)) before the expert loads, plus one import and one weights-free unit test; no load, load order, device, dtype, decode tiling or allocation changed - two report events and nothing else, which is the reports clause of this class. gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus its test - a contract pricing walker Bernini never calls (grep: no reference in mlx-gen-bernini/src), and this is a dense bf16 tier besides. mlx-gen-wan/src/config.rs: the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test, consumed only by mlx-gen-wan memory_strategy.rs canonical_config, the TI2V-5B contract identity Bernini never calls. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (+174 lines under mod tests). mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test - Bernini does not link mlx_gen_wan::memory_strategy. mlx-gen-wan/src/model.rs: load_t2v_14b seals the T2V-A14B receipt and a14b_decode_tiling wraps Wan14b::generate_impl decode tiling with the tile_vae_decode door - Bernini uses neither Wan14b nor either function (its decode tiling is mlx_gen_bernini::memory_strategy::decode_tiling or TilingConfig::auto, bernini.rs:1099-1106); the mlx_gen_wan::model items it does use (effective_te_quant, the VAE and transformer building blocks) are untouched. The z16 decode tile budget the stop was reached in - mlx-gen-wan/src/pipeline.rs auto_tiling_budgeted_z16* and mlx-gen/src/vae_tiling.rs, the budget computed before the KV cache exists - is not in the diff at all, so the decode that tripped the guard is byte-identical at e16c6a55. Every other closure file (gen-core, mlx-gen, core-llm, the rest of mlx-gen-wan and mlx-gen-bernini) is unchanged across the range. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (117 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (117 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed except bernini.rs, whose only change is two Progress::Loading emissions (instrumentation). The peak-reaching path (dual-expert denoise then z16 VAE decode) is byte-identical, so a re-measure could only reproduce the stop. Deliberately NOT written for the sibling bounds recorded on story/sc-22738-mlx-rerun (bernini q4/q8, bernini_image bf16, krea_realtime_14b x3): their records are not in this tree.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan contract walker Bernini never calls; bf16 is a dense tier)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs",
          "class": "accounting-only / instrumentation (two Progress::Loading emissions before the planner and expert loads, one import, one weights-free test; no load, order, device, tiling or allocation changed)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity, never by Bernini)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; Bernini does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (load_t2v_14b seals the T2V-A14B receipt; a14b_decode_tiling wraps Wan14b decode tiling; Bernini uses neither Wan14b nor either function, and the items it uses are untouched)"
        }
      ]
    },
    {
      "anchorId": "exceeded:flux2_dev:mlx:bf16:base:base:sc-18218-flux2-dev-t2i-resident-evidence-v1:exc-2c3953f5b6d3a4680b35",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): flux2_dev:bf16:mlx at 512x512x1 ended in the process-scoped Metal submissions-ignored refusal at a sampled footprint of 86,988,010,336 bytes against the 87,044,670,532-byte wired limit of a 137,438,953,472-byte host at 3b922bac. The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that peak, so a re-run under e16c6a55 would re-establish the same inequality. 3b922bac..e16c6a55 changes eleven inference files; the intersection with this closure (flux2_dev:mlx, 137 files) is exactly one, crates/contracts/gen-core/src/residency.rs, which mlx-gen-flux2 does drive (Residency::from_policy in model_control.rs, Residency::sequential; the bf16 resident load runs the warm arm). The diff is instrumentation-only, the same reading PR #2780 recorded for the five attested anchors: ensure_warm_locked drops its on_progress parameter and no longer emits Loading(TextEncoder)/Loading(Renderer) itself; the warm (!stage_residency) arms of run_request_scoped and run_staged_request_scoped emit Loading(TextEncoder) before the warm load and Loading(Renderer) after the prompt encode, gated on the new warm_needs_load; 147 of the 203 changed lines are unit tests. The components loaded, their order, their devices, the offload policy and every allocation are byte-identical, so the resident bf16 working set that reached the wired limit is the same working set at e16c6a55. The remaining ten changed files (gen-core wan_i2v_memory.rs, mlx-gen-bernini, mlx-gen-mage, mlx-gen-wan, one mlx-gen-flux real-weight test) are outside this closure. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (137 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (137 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: the one changed closure file moves where two progress events are reported and nothing about what is loaded or how much memory it takes. Deliberately NOT extended to any bound recorded on story/sc-22738-mlx-rerun: those records are not in this tree.",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/residency.rs",
          "class": "accounting-only / instrumentation (Loading(TextEncoder)/Loading(Renderer) emission moved from ensure_warm_locked into the warm arms, gated on warm_needs_load; loads, order, devices, offload policy unchanged; 147 lines of new tests)"
        }
      ]
    },
    {
      "anchorId": "exceeded:bernini:mlx:q4:base:base:bernini-image-q4-mlx-dual-expert-ladder-v1:exc-3ff33b42be1429719760",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): bernini:q4:mlx (text_to_video) at 848x480x49 was stopped by the footprint watchdog at 108,484,649,848 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 6 files, read one by one. crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs is the generator body this bound was measured in and the only executed file that changed: generate_impl gains on_progress(Progress::Loading(LoadPhase::TextEncoder)) before the planner load and on_progress(Progress::Loading(LoadPhase::Renderer)) before the expert loads, plus one import (LoadPhase) and one weights-free unit test; no load, load order, device, dtype, decode tiling or allocation changed - two report events and nothing else. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. Bernini reaches none of the changed items: mlx-gen-bernini/src has no reference to wan_i2v_memory (grep: 0), does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy, uses neither Wan14b nor load_t2v_14b nor a14b_decode_tiling (its decode tiling is mlx_gen_bernini::memory_strategy::decode_tiling / TilingConfig::auto), and the mlx_gen_wan items it does import (WanBlockStream, WanTransformer, WanVae, RopeTable, config::WanModelConfig, convert::*, pipeline::denoise_moe / preprocess_* / video_decode_peak_bytes_for_vae_and_tiling, scheduler, text_encoder, model::Wan) are unchanged across the range. The z16 VAE decode tile budget - mlx-gen-wan/src/pipeline.rs auto_tiling_budgeted_z16* and mlx-gen/src/vae_tiling.rs - is not in the diff. Every other closure file (gen-core, mlx-gen, core-llm, the rest of mlx-gen-wan and mlx-gen-bernini) is unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (117 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (117 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker Bernini never calls - grep: 0 references in mlx-gen-bernini/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs",
          "class": "accounting-only / instrumentation (two Progress::Loading emissions before the planner and expert loads, one import, one weights-free test; no load, order, device, tiling or allocation changed)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; WanModelConfig untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; Bernini does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling wraps Wan14b decode tiling; load_t2v_14b seals the T2V-A14B receipt; Bernini uses neither Wan14b nor either function, and the items it uses are untouched)"
        }
      ]
    },
    {
      "anchorId": "exceeded:bernini:mlx:q8:base:base:bernini-image-q8-mlx-dual-expert-ladder-v1:exc-086e91ed15c7415c4ffc",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): bernini:q8:mlx (text_to_video) at 848x480x49 was stopped by the footprint watchdog at 108,595,791,424 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 6 files, read one by one. crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs is the generator body this bound was measured in and the only executed file that changed: generate_impl gains on_progress(Progress::Loading(LoadPhase::TextEncoder)) before the planner load and on_progress(Progress::Loading(LoadPhase::Renderer)) before the expert loads, plus one import (LoadPhase) and one weights-free unit test; no load, load order, device, dtype, decode tiling or allocation changed - two report events and nothing else. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. Bernini reaches none of the changed items: mlx-gen-bernini/src has no reference to wan_i2v_memory (grep: 0), does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy, uses neither Wan14b nor load_t2v_14b nor a14b_decode_tiling (its decode tiling is mlx_gen_bernini::memory_strategy::decode_tiling / TilingConfig::auto), and the mlx_gen_wan items it does import (WanBlockStream, WanTransformer, WanVae, RopeTable, config::WanModelConfig, convert::*, pipeline::denoise_moe / preprocess_* / video_decode_peak_bytes_for_vae_and_tiling, scheduler, text_encoder, model::Wan) are unchanged across the range. The z16 VAE decode tile budget - mlx-gen-wan/src/pipeline.rs auto_tiling_budgeted_z16* and mlx-gen/src/vae_tiling.rs - is not in the diff. Every other closure file (gen-core, mlx-gen, core-llm, the rest of mlx-gen-wan and mlx-gen-bernini) is unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (117 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (117 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker Bernini never calls - grep: 0 references in mlx-gen-bernini/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs",
          "class": "accounting-only / instrumentation (two Progress::Loading emissions before the planner and expert loads, one import, one weights-free test; no load, order, device, tiling or allocation changed)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; WanModelConfig untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; Bernini does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling wraps Wan14b decode tiling; load_t2v_14b seals the T2V-A14B receipt; Bernini uses neither Wan14b nor either function, and the items it uses are untouched)"
        }
      ]
    },
    {
      "anchorId": "exceeded:bernini_image:mlx:bf16:base:base:bernini-image-bf16-mlx-dual-expert-ladder-v1:exc-55ce757827be31846a13",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): bernini_image:bf16:mlx (text_to_image; the bernini_image:mlx closure is the same 117 files as bernini:mlx, differing only in its entry points) at 848x480x1 was stopped by the footprint watchdog at 94,852,699,512 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 6 files, read one by one. crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs is the generator body this bound was measured in and the only executed file that changed: generate_impl gains on_progress(Progress::Loading(LoadPhase::TextEncoder)) before the planner load and on_progress(Progress::Loading(LoadPhase::Renderer)) before the expert loads, plus one import (LoadPhase) and one weights-free unit test; no load, load order, device, dtype, decode tiling or allocation changed - two report events and nothing else. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. Bernini reaches none of the changed items: mlx-gen-bernini/src has no reference to wan_i2v_memory (grep: 0), does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy, uses neither Wan14b nor load_t2v_14b nor a14b_decode_tiling (its decode tiling is mlx_gen_bernini::memory_strategy::decode_tiling / TilingConfig::auto), and the mlx_gen_wan items it does import (WanBlockStream, WanTransformer, WanVae, RopeTable, config::WanModelConfig, convert::*, pipeline::denoise_moe / preprocess_* / video_decode_peak_bytes_for_vae_and_tiling, scheduler, text_encoder, model::Wan) are unchanged across the range. The z16 VAE decode tile budget - mlx-gen-wan/src/pipeline.rs auto_tiling_budgeted_z16* and mlx-gen/src/vae_tiling.rs - is not in the diff. Every other closure file (gen-core, mlx-gen, core-llm, the rest of mlx-gen-wan and mlx-gen-bernini) is unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (117 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (117 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker Bernini never calls - grep: 0 references in mlx-gen-bernini/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-bernini/src/bernini.rs",
          "class": "accounting-only / instrumentation (two Progress::Loading emissions before the planner and expert loads, one import, one weights-free test; no load, order, device, tiling or allocation changed)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; WanModelConfig untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; Bernini does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling wraps Wan14b decode tiling; load_t2v_14b seals the T2V-A14B receipt; Bernini uses neither Wan14b nor either function, and the items it uses are untouched)"
        }
      ]
    },
    {
      "anchorId": "exceeded:krea_realtime_14b:mlx:bf16:base:base:krea-realtime-14b-bf16-mlx-resident-ladder-v1:exc-596a1ae39ba4e61551cd",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): krea_realtime_14b:bf16:mlx (text_to_video) at 832x480x45 was stopped by the footprint watchdog at 109,921,567,216 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 5 files, read one by one. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. krea_realtime_14b reaches none of the changed items: mlx-gen-krea-realtime/src has no reference to wan_i2v_memory, Wan14b, load_t2v_14b or load_i2v_14b (grep: 0); from mlx_gen_wan::model it uses only effective_te_quant (t2v.rs:59, :995) and descriptor_t2v_14b (pipeline.rs:881), both untouched; its decode tiling is mlx_gen_wan::pipeline::auto_tiling_budgeted_z16_quality_overlap (t2v.rs:60, :292), and pipeline.rs is not in the diff; from config.rs it imports WanModelConfig / WanQuant / GuideScale, which the diff does not touch; it does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy. Its own crate, gen-core residency.rs aside (the instrumentation-only move of two Progress::Loading emissions recorded for the anchors at this pin, and not in this closure's intersection), and every other closure file are unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (105 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (105 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker krea_realtime_14b never calls - grep: 0 references in mlx-gen-krea-realtime/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; the WanModelConfig / WanQuant / GuideScale items krea-realtime imports are untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; krea-realtime does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling and load_t2v_14b are Wan14b's own; krea-realtime takes only effective_te_quant and descriptor_t2v_14b from this file, both untouched, and tiles its decode through the untouched pipeline.rs auto_tiling_budgeted_z16_quality_overlap)"
        }
      ]
    },
    {
      "anchorId": "exceeded:krea_realtime_14b:mlx:q4:base:base:krea-realtime-14b-q4-mlx-resident-ladder-v1:exc-c8c7f41ad2c9c1f42023",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): krea_realtime_14b:q4:mlx (text_to_video) at 832x480x45 was stopped by the footprint watchdog at 110,079,800,312 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 5 files, read one by one. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. krea_realtime_14b reaches none of the changed items: mlx-gen-krea-realtime/src has no reference to wan_i2v_memory, Wan14b, load_t2v_14b or load_i2v_14b (grep: 0); from mlx_gen_wan::model it uses only effective_te_quant (t2v.rs:59, :995) and descriptor_t2v_14b (pipeline.rs:881), both untouched; its decode tiling is mlx_gen_wan::pipeline::auto_tiling_budgeted_z16_quality_overlap (t2v.rs:60, :292), and pipeline.rs is not in the diff; from config.rs it imports WanModelConfig / WanQuant / GuideScale, which the diff does not touch; it does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy. Its own crate, gen-core residency.rs aside (the instrumentation-only move of two Progress::Loading emissions recorded for the anchors at this pin, and not in this closure's intersection), and every other closure file are unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (105 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (105 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker krea_realtime_14b never calls - grep: 0 references in mlx-gen-krea-realtime/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; the WanModelConfig / WanQuant / GuideScale items krea-realtime imports are untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; krea-realtime does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling and load_t2v_14b are Wan14b's own; krea-realtime takes only effective_te_quant and descriptor_t2v_14b from this file, both untouched, and tiles its decode through the untouched pipeline.rs auto_tiling_budgeted_z16_quality_overlap)"
        }
      ]
    },
    {
      "anchorId": "exceeded:krea_realtime_14b:mlx:q8:base:base:krea-realtime-14b-q8-mlx-resident-ladder-v1:exc-e3635aa610448bc8d98a",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): krea_realtime_14b:q8:mlx (text_to_video) at 832x480x45 was stopped by the footprint watchdog at 110,094,811,152 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 5 files, read one by one. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. krea_realtime_14b reaches none of the changed items: mlx-gen-krea-realtime/src has no reference to wan_i2v_memory, Wan14b, load_t2v_14b or load_i2v_14b (grep: 0); from mlx_gen_wan::model it uses only effective_te_quant (t2v.rs:59, :995) and descriptor_t2v_14b (pipeline.rs:881), both untouched; its decode tiling is mlx_gen_wan::pipeline::auto_tiling_budgeted_z16_quality_overlap (t2v.rs:60, :292), and pipeline.rs is not in the diff; from config.rs it imports WanModelConfig / WanQuant / GuideScale, which the diff does not touch; it does not link mlx_gen_wan::memory_strategy or i2v_memory_strategy. Its own crate, gen-core residency.rs aside (the instrumentation-only move of two Progress::Loading emissions recorded for the anchors at this pin, and not in this closure's intersection), and every other closure file are unchanged. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (105 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (105 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in packed_transformer_bytes plus test; a Wan I2V contract walker krea_realtime_14b never calls - grep: 0 references in mlx-gen-krea-realtime/src)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity; the WanModelConfig / WanQuant / GuideScale items krea-realtime imports are untouched)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`; krea-realtime does not link this module)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only for this bound (a14b_decode_tiling and load_t2v_14b are Wan14b's own; krea-realtime takes only effective_te_quant and descriptor_t2v_14b from this file, both untouched, and tiles its decode through the untouched pipeline.rs auto_tiling_budgeted_z16_quality_overlap)"
        }
      ]
    },
    {
      "anchorId": "exceeded:wan_2_2_i2v_14b:mlx:bf16:base:base:sc-22736-wan2-2-i2v-a14b-mlx-dense-v1:exc-8071db4312d55971c74b",
      "measuredRevision": "3b922bac6094e06f98bb2598a6d6fe10dc92739e",
      "attestedRevision": "81fda3bd5a9d5920ad9cdc62796df3305be96742",
      "attestedAt": "2026-09-10",
      "story": "sc-22738, sc-23026",
      "class": "accounting-only",
      "why": "A MEASURED LOWER BOUND, not an anchor (sc-22738): wan_2_2_i2v_14b:bf16:mlx (image_to_video) at 1280x720x77 was stopped by the footprint watchdog at 106,174,403,704 bytes against the 94,822,600,832-byte ceiling of a 137,438,953,472-byte host at 3b922bac (the record names the ceiling, the observed footprint and the geometry; it does not name the phase the stop landed in, and this attestation claims nothing about that). The claim attested is that the closure diff 3b922bac..e16c6a55 cannot change that render's peak, so a re-run under e16c6a55 would re-establish the same inequality and the probe tooling should keep the cell exceeded_current rather than book the same guarded render again. 3b922bac..e16c6a55 changes eleven inference files (gen-core residency.rs and wan_i2v_memory.rs; mlx-gen-bernini bernini.rs and its conformance test; mlx-gen-mage model.rs and pipeline.rs; mlx-gen-wan config.rs, i2v_memory_strategy.rs, memory_strategy.rs and model.rs; one mlx-gen-flux real-weight test). The intersection with this closure is 5 files, read one by one. crates/contracts/gen-core/src/wan_i2v_memory.rs: packed_transformer_bytes skips a float 2-D weight carrying no quant leaves (the dense output head on PACKED Wan tiers) plus a test; it is the (MemoryPhase::Denoise, Some(quant)) pricing arm of the Wan I2V contract walker (wan_i2v_memory.rs:1429), reached only for a packed tier. mlx-gen-wan/src/config.rs: adds the LEGACY_INERT_CONFIG_KEYS constant and without_legacy_inert_keys plus a test; WanModelConfig and WanQuant, the items other crates import from it, are untouched. mlx-gen-wan/src/memory_strategy.rs: canonical_config drops the inert max_area key before the TI2V-5B identity comparison plus a test; canonical_config is reached only from memory_strategy_contract / resolved_numeric_tier, which model.rs calls from `load` (line 441, MODEL_ID = wan2_2_ti2v_5b) and nowhere else. mlx-gen-wan/src/i2v_memory_strategy.rs: tests only (the single hunk starts at line 659, inside #[cfg(test)] mod tests). mlx-gen-wan/src/model.rs: three changes - a new a14b_decode_tiling that wraps Wan14b::generate_impl's existing decode-tiling selection (its image_to_video-with-memory arm is the unchanged i2v_memory_strategy::decode_tiling call, and the new tile_vae_decode door only widens the same call), load_t2v_14b sealing an i2v_memory receipt when resolved_route == wan2_2_t2v_14b, and generate_impl calling the wrapper. effective_te_quant, descriptor_t2v_14b, the Wan type, WanTransformer, WanVae and every other item outside those three functions are byte-identical. The wan_2_2_i2v_14b route loads through load_i2v_14b (model.rs:2214), which calls i2v_memory_strategy::prepare(spec, MODEL_ID_I2V_14B) - unchanged, since that file's diff is tests only - and renders through Wan14b::generate_impl, whose decode tiling is now selected by a14b_decode_tiling: for an image_to_video request the arm taken is the same i2v_memory_strategy::decode_tiling call whether or not the request carries memory.tile_vae_decode, and a request without a memory contract falls to auto_tiling_budgeted_z16 at both revisions, so the selection is identical for every I2V input. load_t2v_14b's receipt seal is the T2V route. packed_transformer_bytes prices packed tiers only and this is the dense bf16 tier (tier.quant is None, the dense arm, unchanged). canonical_config is the TI2V-5B loader's. The z16 decode tile budget itself (pipeline.rs auto_tiling_budgeted_z16*, mlx-gen/src/vae_tiling.rs) is not in the diff. EXTENDED to e34d7b46 (sc-22738, fifth terminal pin; inference main = PR #966 fix/sc-22738-sensenova-measurable over e16c6a55): the e16c6a55..e34d7b46 range changes five inference files, all under crates/media/mlx-gen/mlx-gen-sensenova (src/memory_strategy.rs, src/model.rs, src/t2i.rs and the two parity tests), and the intersection with this closure's closureFiles (95 files) is EMPTY - the closure is byte-identical across the range and derives the same currency key at e34d7b46 as at e16c6a55. The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. EXTENDED to e11fd9f0 (sc-22738, sixth terminal pin; inference main = PR #967 fix/sc-22738-candle-kolors-ideogram-predicates over e34d7b46, its first parent): the e34d7b46..e11fd9f0 range changes exactly three inference files, all a candle-gen memory_strategy.rs - candle-gen-kolors (dense/non-packed tensors may be F16 as well as BF16, since the shipped rehost's shards are F16 and every dense tensor is read at F32), candle-gen-ideogram (the VAE's rank-0 I32/I64 BatchNorm counter is priced at zero, and the turbo id stops refusing the co-located unconditional_transformer/ its loader never opens) and candle-gen-bernini (contract_for_loaded and validate_loaded_contract publish the calibration identity memory_strategy_contract already computed). The intersection with this closure's closureFiles (95 files) is EMPTY, and stronger than empty: this closure reaches no file of ANY of the three crates (grep over closureFiles for candle-gen-kolors / candle-gen-ideogram / candle-gen-bernini: 0 hits), so the closure is byte-identical across the range and derives the same currency key at e11fd9f0 as at e34d7b46 (config/anchor-loader-closures.json records the same digest for this model at both pins; only bernini:candle, bernini_image:candle, ideogram_4:candle, ideogram_4_turbo:candle and kolors:candle moved). The trivial extension the doctrine names; no file is added to filesChangedSinceMeasurement and no re-measure was taken. sc-23026 pin extension (e11fd9f0fd26..81fda3bd5a9d): reviewed the changed inference files (crates/media/candle-gen/candle-gen-flux2/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/lib.rs, crates/media/candle-gen/candle-gen-ltx/src/memory_strategy.rs, crates/media/candle-gen/candle-gen-minimax-h3/src/memory_strategy.rs). None intersects this model/backend's old or new closureFiles, and its derived loader digest is unchanged. The existing witness and accounting classification therefore extend without another measurement.",
      "witness": "None, and none is needed under the doctrine: no file this bound loads or executes changed in a way that alters what is loaded, in what order, on which device, at what dtype, or how the decode is tiled; the changed lines it can reach are progress-report instrumentation, pricing for tiers or routes it does not take, the TI2V-5B contract identity, tests, and a behaviour-preserving wrapper. Assembled in the campaign evidence PR beside the record it cites (the same reading PR #2782 recorded for the bernini bf16 and flux2_dev bf16 bounds and deliberately withheld there because these records were not yet in that tree).",
      "filesChangedSinceMeasurement": [
        {
          "path": "crates/contracts/gen-core/src/wan_i2v_memory.rs",
          "class": "accounting-only (packed-tier dense-head pricing in the (Denoise, Some(quant)) arm of packed_transformer_bytes plus test; bf16 is the dense tier and takes the unchanged dense arm)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/config.rs",
          "class": "accounting-only (LEGACY_INERT_CONFIG_KEYS + without_legacy_inert_keys + test; consumed only by the TI2V-5B contract identity)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/i2v_memory_strategy.rs",
          "class": "test-only (the I2V-14B loader's prepare and decode_tiling are byte-identical)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/memory_strategy.rs",
          "class": "accounting-only (canonical_config tolerates the inert max_area key for the TI2V-5B contract + test; reached only from the wan2_2_ti2v_5b `load`, not from load_i2v_14b)"
        },
        {
          "path": "crates/media/mlx-gen/mlx-gen-wan/src/model.rs",
          "class": "accounting-only / behaviour-preserving wrapper for this bound (a14b_decode_tiling selects the same i2v_memory_strategy::decode_tiling call for every image_to_video request that the inline selection did; load_t2v_14b's receipt seal is the T2V route)"
        }
      ]
    }
  ]
}
