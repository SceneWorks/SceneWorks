[pytest]
markers =
    parity: runs the Rust API against isolated temp data and compares wire contracts to snapshots
    e2e: drives a job end to end through the Rust API binary and the in-process Python worker (procedural adapter, no model weights)
